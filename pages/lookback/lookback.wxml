<view class='header' style='width: 100%;height: 100%;display: flex;align-items: center;'>


  <view class="page-section page-section-spacing swiper">

    <swiper style="height:650rpx ">

      <block wx:for="{{thisQustions}}" wx:key="*this">
        <swiper-item>

          <view class='answerarea ' style='width:80%; '>
          <text>{{item.topic}}</text>

            <view wx:for="{{item.answers}}" data-index="{{index}}"  class="{{index == 0  ? 'right':'' }}  answer ">

              <view class="invisible {{index == 0}}" >○</view>
              <!-- <view class="invisible {{index==click_index&&click_index!==''&&answer_color=='error' ? 'sign' : ''}}">×</view> -->
              
              <view style='width:100%;'>{{item}}</view>


            </view>
          </view>


        </swiper-item>
      </block>

    </swiper>
  </view>


</view>